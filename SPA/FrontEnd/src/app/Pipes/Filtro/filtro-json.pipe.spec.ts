import { FiltroJSONPipe } from './filtro-json.pipe';

describe('FiltroJSONPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroJSONPipe();
    expect(pipe).toBeTruthy();
  });
});

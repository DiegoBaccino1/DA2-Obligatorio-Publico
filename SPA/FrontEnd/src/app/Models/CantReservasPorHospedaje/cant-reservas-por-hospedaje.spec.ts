import { CantReservasPorHospedaje } from './cant-reservas-por-hospedaje';

describe('CantReservasPorHospedaje', () => {
  it('should create an instance', () => {
    expect(new CantReservasPorHospedaje()).toBeTruthy();
  });
});

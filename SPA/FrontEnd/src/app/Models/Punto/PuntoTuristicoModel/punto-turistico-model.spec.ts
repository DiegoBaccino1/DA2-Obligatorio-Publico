import { PuntoTuristicoModel } from './punto-turistico-model';

describe('PuntoTuristicoModel', () => {
  it('should create an instance', () => {
    expect(new PuntoTuristicoModel()).toBeTruthy();
  });
});

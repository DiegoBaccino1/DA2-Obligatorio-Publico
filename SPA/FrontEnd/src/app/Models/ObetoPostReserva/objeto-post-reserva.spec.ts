import { ObjetoPostReserva } from './objeto-post-reserva';

describe('ObjetoPostReserva', () => {
  it('should create an instance', () => {
    expect(new ObjetoPostReserva()).toBeTruthy();
  });
});
